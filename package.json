{
  "name": "monitor-server-listener",
  "version": "1.0.0",
  "description": "check for monitor-server-listener response",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "maty zisserman",
  "license": "MIT",
  "dependencies": {
    "@hkube/kubernetes-client": "^1.0.25",
    "socket.io": "^2.2.0",
    "socket.io-client": "^2.2.0"
  }
}
