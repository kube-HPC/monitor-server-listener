{
	"name": "monitor-server-listener",
	"version": "2.0.0",
	"description": "check for monitor-server-listener response",
	"main": "app.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 0",
		"build": "dockerfile/build.sh monitor-server-listener"
	},
	"author": "maty zisserman",
	"license": "MIT",
	"dependencies": {
		"@hkube/kubernetes-client": "^2.0.3",
		"socket.io": "^3.0.0",
		"socket.io-client": "^4.0.0"
	}
}
